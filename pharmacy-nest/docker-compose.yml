version: '3.9'

services:
  backend:
    build: .
    ports:
      - "5001:5001"
    restart: always

  frontend:
    build: ../frontend/pharmacy-frontend-v2
    ports:
      - "3001:3000"
    depends_on:
      - backend
    restart: always